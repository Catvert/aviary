/target
*.swp
.DS_Store
.env
.env.*
!.env.example

# `nix build` output symlink.
/result
/result-*

# Per-developer worktree tooling (github.com/…/wt), not part of the project.
/wt.toml

# Release tarballs assembled by packaging/install.sh consumers.
/aviary-*.tar.gz

# Working notes kept outside the published tree: the roadmap and the design
# write-ups that go with it are drafts about work not done yet, revised far
# more often than the code they describe. `CLAUDE.md` documents what exists.
/TODO.md
/docs/
